One-line: the floating action button for the screen's single most important action.

```jsx
<Fab icon="add" label="문의하기" />
<Fab icon="edit" size="small" color="secondary" />
<Fab icon="chat" size="large" />
```

`label` makes it an extended FAB. Sizes: small, regular, large. Colors: primary, secondary, surface.
