One-line: a circular user/brand image with an initials fallback.

```jsx
<Avatar src="/team/jane.jpg" alt="제인" />
<Avatar initials="EC" size={56} />
```

Provide `src` for a photo or `initials` for the fallback. `size` controls diameter.
