One-line: a thin rule that separates content groups or list items.

```jsx
<Divider />
<Divider inset />
```

Use `inset` to indent from the left (e.g. aligned under list text), `vertical` for column separators.
