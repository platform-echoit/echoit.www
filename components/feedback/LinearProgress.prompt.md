One-line: a horizontal progress bar for determinate or ongoing tasks.

```jsx
<LinearProgress value={64} />
<LinearProgress /> {/* indeterminate */}
```

Pass `value` (0–100) for determinate; omit for an animated indeterminate bar.
