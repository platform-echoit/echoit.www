One-line: a brief, low-emphasis confirmation message at the edge of the screen.

```jsx
<Snackbar message="문의가 전송되었습니다" action="실행취소" onAction={undo} />
```

Optional `action` button and `onClose` affordance. Uses the inverse surface colors.
