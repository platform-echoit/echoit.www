One-line: a small count or status dot, on its own or anchored over an icon.

```jsx
<Badge count={3} />
<Badge dot anchored={<Icon name="notifications" />} />
```

Use `dot` for a presence indicator, `count` for a number, and `anchored` to overlay it on another element.
